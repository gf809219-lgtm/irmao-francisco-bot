services:
  - type: web
    name: irmao-francisco-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python padre_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false